services:
  - type: web
    name: vexx-gimkit-joiner
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: gunicorn app:app
